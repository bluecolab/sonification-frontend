# Requirements for Sonification Flask
# Prereqs: pip -> setuptools -> wheel
#Requires installation / system environmental path of ffmpeg and FluidSynth (for data convertion from midi to mp3)

#cmd prompt run: pip install -r requirements.txt
#cmd prompt run: pip install -r requirements.txt
Flask==2.1.1
# Matplotlib requires Microsoft Visual C++ 2008 or 2010
matplotlib==3.9.0
MIDIUtil==1.2.1
pandas==2.2.2
plotly==5.6.0
plotnine==0.13.6
pyFluidSynth==1.3.3
